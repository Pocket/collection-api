{
  "name": "collections-api",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "private": true,
  "scripts": {
    "get": "cdktf get",
    "build:dev": "npm run build && NODE_ENV=development npm run synth",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "upgrade": "npm i cdktf@latest cdktf-cli@latest",
    "upgrade:next": "npm i cdktf@next cdktf-cli@next"
  },
  "engines": {
    "node": "=14"
  },
  "dependencies": {
    "@pocket/terraform-modules": "1.13.0",
    "cdktf": "0.0.17",
    "constructs": "3.3.71"
  },
  "devDependencies": {
    "@types/node": "14.14.37",
    "cdktf-cli": "0.2.1",
    "typescript": "4.2.3"
  }
}

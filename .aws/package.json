{
  "name": "collections-api",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "src/main.ts",
  "private": true,
  "scripts": {
    "build:dev": "rm -rf dist && NODE_ENV=development npm run synth",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "3.10.0"
  },
  "devDependencies": {
    "@types/node": "16.11.35",
    "typescript": "4.6.4"
  }
}
